name: Update Submodule

on:
  repository_dispatch:
    types: [update-submodule]
  workflow_dispatch:
  schedule:
    - cron: '23 3 * * *'

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v6
        with:
          ref: v13
          submodules: 'recursive'

      - name: Update submodule
        run: |
          git submodule update --remote content/GreenStack.Umbraco.UmBootstrap
          echo "CHANGED=$(git diff --quiet && echo false || echo true)" >> $GITHUB_ENV

      - name: Create Pull Request
        if: env.CHANGED == 'true'
        uses: peter-evans/create-pull-request@v8
        with:
          base: v13
          commit-message: "chore: update GreenStack.Umbraco.UmBootstrap-v13 submodule"
          title: "Update GreenStack.Umbraco.UmBootstrap-v13 submodule"
          body: "Automated submodule update from [GreenStack.Umbraco.UmBootstrap-v13](https://github.com/UmbHost/GreenStack.Umbraco.UmBootstrap-v13)."
          branch: update-submodule-v13
          delete-branch: true
